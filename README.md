# Word2vecfromScratch
 Complete implementation of Word2Vec using skip-gram architecture with negative sampling. Built entirely in Python/NumPy without external ML libraries. Features vocabulary building, unigram table generation, gradient descent training, and cosine similarity search. Includes sample corpus and model persistence.
